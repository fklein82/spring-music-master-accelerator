#@ load("@ytt:data", "data")
---
primary:
  persistence:
    enabled: true
    storageClass: "tanzu-sp"

auth:
  database: music
  username: "postgres"

global:
  postgresql:
    auth:      
      existingSecret: #@ data.values.secretName